# CLAUDE.md

日本大学習志野高等学校の紹介サイト。模擬講義中に、生徒の声を聞きながらその場で書き換える。

## 構成

- ビルド不要の静的サイト。`index.html` をブラウザで開くだけ。
- `index.html` は空の器。見出しも本文も `script.js` が流し込む。
- 文面・色・ゲームの内容はすべて `script.js` 冒頭の `siteData` にある。**内容の変更は `siteData` だけを編集する。** その下のレンダリング関数、SVG生成、ゲームエンジンは触らない。
- `styles.css` は編集的なタイポグラフィ設計。明朝（Shippori Mincho B1）が見出し、ゴシック（Zen Kaku Gothic New）が本文、等幅（DM Mono）がラベル。色は `--ink` `--paper` `--brand` `--accent` の4系統。
- `siteData.theme.brand` と `accent` は JS から CSS 変数に反映される。色を変えるならここ。

## 書き方

- 生徒名や個人が特定できる情報は書かない。生徒の声は「1年生」名義にする。
- 公式サイトから分かる事実は `notes/nichidai-narashino-research.md` にある。分からないことは推測で書かず、そのまま聞く。
- 日本語で、短く、高校生が読んで楽しい文にする。見出しは体言止めか短い言い切り。
- 生徒の声を入れるときは、その `voices` 項目の `placeholder: true` を消す（枠が点線から実線になる）。
- 編集後は `node --check script.js` で構文を確認する。

## ミニゲーム「日習クエスト」

横スクロールアクション。ステージは `siteData.game.stages[].map` の文字列配列で、1文字が1マス（40px）。

| 記号 | 意味 |
| --- | --- |
| `.` | 空 |
| `#` | 地面・ブロック（ぶつかる） |
| `=` | すり抜け床（下から通れて、上には乗れる） |
| `^` | トゲ（ダメージ） |
| `o` | アイテム |
| `E` | 歩く敵（上から踏むと倒せる） |
| `F` | 飛ぶ敵（上から踏むと倒せる） |
| `M` | 横に動く床。右に続く `-` が動く範囲 |
| `V` | 縦に動く床。上下に続く `\|` が動く範囲 |
| `B` | ジャンプ台 |
| `C` | 中間地点（ミスするとここから再開） |
| `P` | スタート（1ステージに1つ） |
| `G` | ゴール（1ステージに1つ） |

### ステージを書き換えるときのルール

- **1つのステージの全行を同じ長さにする。** 長さが違うと動かない。
- 登れるのは**3マス上まで**。4マス以上の段差は登れないので、途中に足場を置くか `B` を使う。
- 飛び越せる穴は**3マスまで**。4マス以上は動く床を置く。
- `B` のジャンプ台なら**6マス上まで**届く。
- `P` と `G` はそれぞれちょうど1つ。

### 書き換えたら必ず実行

```
node tools/check-stages.mjs
```

行の長さ、`P` と `G` の数、そして **P からG まで本当にたどり着けるか** を自動で確かめます。`NG` が出たらステージを直す。

### そのほかの調整

- `siteData.game.physics` でジャンプ力（`jump`）、重力（`gravity`）、速さ（`maxSpeed`）、ジャンプ台の強さ（`pad`）を変えられる。
- `siteData.game.lives` で残機、`stages[].palette` で色、`stages[].items` でアイテムの文字、`stages[].enemyLabels` で敵の名前を変えられる。
- ステージを増やすときは `stages` に要素を足すだけ。`legend` と画面の一覧は自動で追従する。
